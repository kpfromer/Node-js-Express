extends layout

block content
    section#content
        ul(style={background: 'black'})
            each color in colors
                li(style={color: color})= color
        h2= prompt
        if hint
            p
                i Hint: #{hint}